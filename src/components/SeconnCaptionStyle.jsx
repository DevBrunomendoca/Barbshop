import styled from "styled-components"

export const SecondCaptionStyled = styled.h2`
  font-size:2.5rem;
  color: var(--primary-color);
  //font-family: 'Lora', serif;
  font-family: 'Poppins', serif;
  line-height: 100%;
  font-weight: 600;
  @media (max-width:600px) {
    font-size: 2.2rem;
  }
`